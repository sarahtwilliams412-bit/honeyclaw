apiVersion: v1
kind: Service
metadata:
  name: {{ include "honeyclaw.fullname" . }}
  labels:
    {{- include "honeyclaw.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- range .Values.service.ports }}
    - name: {{ .name }}
      port: {{ .port }}
      targetPort: {{ .targetPort }}
      protocol: {{ .protocol }}
    {{- end }}
  selector:
    {{- include "honeyclaw.selectorLabels" . | nindent 4 }}
